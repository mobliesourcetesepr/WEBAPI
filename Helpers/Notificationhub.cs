using Microsoft.AspNetCore.SignalR;

namespace AgentCreation.Hubs
{
    public class NotificationHub : Hub
    {
        // Optional: override OnConnectedAsync or OnDisconnectedAsync if needed
    }
}
